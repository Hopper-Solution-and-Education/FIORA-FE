export const DEFAULT_LOCALE = 'vi-VN';
export const DEFAULT_CURRENCY = 'VND';
export const DEFAULT_CHILD_OPACITY = 0.6; // Slightly higher for visibility (was 0.5)
export const DEFAULT_MAX_BAR_RATIO = 0.9; // 90% of chart width for largest bar
export const CHART_MARGINS = { top: 10, right: 30, left: 100, bottom: 30 };
export const BASE_BAR_HEIGHT = 40; // Minimum height per bar in pixels
export const MIN_CHART_HEIGHT = 200; // Minimum height in pixels
export const COLORS = {
  Expense: '#EF4444', // Red for expenses
  Income: '#10B981', // Green for income
};
