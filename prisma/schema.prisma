datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["linux-musl-arm64-openssl-3.0.x"]
}

model User {
  id             String               @id @default(uuid()) @db.Uuid
  name           String?
  email          String               @unique
  password       String?
  emailVerified  Boolean?             @default(false)
  image          String?
  authentication UserAuthentication[]
  sessions       Session[]
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  accounts       Account[]            @relation("UserAccounts")
  Recommendation Recommendation[]
  categories     Category[]           @relation("UserCategories")
  // BudgetPlan BudgetPlan[]
  // Transaction Transaction[]

  // Limit Limit[]
}

model Account {
  id          String      @id @default(uuid()) @db.Uuid
  userId      String      @db.Uuid
  icon        String?
  name        String      @db.VarChar(50)
  description String?     @db.VarChar(1000)
  type        AccountType @default(Payment)
  currency    Currency    @default(VND)
  limit       Decimal?    @default(0) @db.Decimal(13, 2)
  balance     Decimal?    @default(0) @db.Decimal(13, 2)
  parentId    String?     @db.Uuid
  parent      Account?    @relation("ChildAccounts", fields: [parentId], references: [id])
  children    Account[]   @relation("ChildAccounts")
  user        User        @relation("UserAccounts", fields: [userId], references: [id])
  // Transaction Transaction[] // 

  @@index([userId]) // Index for faster queries on userId
  @@index([parentId]) // Index for faster queries on parentId
}

model UserAuthentication {
  id                String   @id @default(uuid()) @db.Uuid
  userId            String   @unique @db.Uuid
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  user              User     @relation(fields: [userId], references: [id])

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(uuid()) @db.Uuid
  sessionToken String   @unique
  userId       String   @db.Uuid
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id])
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime

  @@id([identifier, token])
}

// Kien - Landing page - kiểu dữ liệu cho image
enum MediaType {
  IMAGE
  VIDEO
  EMBEDDED
}

// Kien - landing page - Section type cho image
enum SectionType {
  BANNER
  VISION_MISSION
  KPS
  PARTNER_LOGO
}

// Kien - Media giữ image ở landing page
model Media {
  id            Int       @id @default(autoincrement())
  media_type    MediaType
  media_url     String? // Dành cho hình ảnh/video
  embed_code    String? // Dành cho video nhúng (YouTube, Vimeo,...)
  description   String?
  uploaded_by   String?
  uploaded_date DateTime  @default(now())
  // Liên kết với Section
  section_id    Int?
  section       Section?  @relation(fields: [section_id], references: [section_id])
}

model Section {
  section_id   Int         @id @default(autoincrement())
  section_type SectionType
  name         String
  order        Int
  created_at   DateTime    @default(now())
  updated_at   DateTime    @updatedAt
  medias       Media[]
}

// Kien - Home - Recomendation
model Recommendation {
  id          String   @id @default(uuid()) // System generated UUID
  userId      String?  @unique @map("user_id") // Optional: if blank, applies to all users
  title       String   @db.VarChar(100) // Title of recommendation
  description String   @db.VarChar(1000) // Recommendation details
  link        String? // Deep link redirection
  image       String? // Embedded image link
  attachments String[] // Array of file links (IMG, PNG, PDF, DOC, Excel, PowerPoint)
  user        User?    @relation(fields: [userId], references: [id], onDelete: Cascade)
}

enum CategoryType {
  Expense
  Income
}

model Category {
  id            String       @id @default(uuid()) @db.Uuid
  userId        String       @db.Uuid
  type          CategoryType
  icon          String
  name          String       @db.VarChar(50)
  description   String?      @db.VarChar(1000)
  parentId      String?      @db.Uuid
  parent        Category?    @relation("SubCategories", fields: [parentId], references: [id], onDelete: Cascade)
  subCategories Category[]   @relation("SubCategories")
  user          User         @relation("UserCategories", fields: [userId], references: [id], onDelete: Cascade)
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  // BudgetItem BudgetItem[]

  // Transaction Transaction[]

  // Limit Limit[]
}

enum AccountType {
  Payment
  Saving
  CreditCard
  Debt
  Lending
}

enum Currency {
  VND
  USD
}

// // Model budget planing - Kiên
// model BudgetPlan {
//   id         String    @id @default(uuid()) @db.Uuid
//   userId     String    @db.Uuid
//   name       String    @db.VarChar(100)  // Tên ngân sách (VD: "Kế hoạch chi tiêu tháng 3")
//   description String?  @db.VarChar(1000)
//   startDate  DateTime  // Ngày bắt đầu ngân sách
//   endDate    DateTime  // Ngày kết thúc ngân sách
//   totalBudget Decimal  @db.Decimal(13,2) // Tổng ngân sách được cấp phát
//   status     BudgetStatus @default(Active) // Trạng thái (Active, Completed, Archived)
//   createdAt  DateTime  @default(now())
//   updatedAt  DateTime  @updatedAt
//   user       User      @relation(fields: [userId], references: [id], onDelete: Cascade)
//   items      BudgetItem[]  // Liên kết đến các mục chi tiêu của ngân sách

//   @@index([userId])
// }

// enum BudgetStatus {
//   Active       // Ngân sách đang được áp dụng
//   Completed    // Ngân sách đã hoàn thành
//   Archived     // Ngân sách đã được lưu trữ (không còn hiệu lực)
//   OverBudget   // Ngân sách đã vượt quá hạn mức
// }

// model BudgetItem {
//   id          String    @id @default(uuid()) @db.Uuid
//   budgetPlanId String   @db.Uuid
//   categoryId  String    @db.Uuid
//   allocated   Decimal   @db.Decimal(13,2) // Ngân sách dự kiến
//   actualSpent Decimal   @default(0) @db.Decimal(13,2) // Số tiền thực chi
//   createdAt   DateTime  @default(now())
//   updatedAt   DateTime  @updatedAt
//   budgetPlan  BudgetPlan @relation(fields: [budgetPlanId], references: [id], onDelete: Cascade)
//   category    Category   @relation(fields: [categoryId], references: [id], onDelete: Cascade)

//   @@index([budgetPlanId])
//   @@index([categoryId])
//   Transaction Transaction[]
// }

// model Transaction {
//   id          String    @id @default(uuid()) @db.Uuid
//   userId      String    @db.Uuid
//   accountId   String    @db.Uuid
//   categoryId  String    @db.Uuid
//   amount      Decimal   @db.Decimal(13,2) // Số tiền của giao dịch
//   type        CategoryType  // Expense hoặc Income
//   description String?   @db.VarChar(1000) // Ghi chú giao dịch
//   date        DateTime  @default(now()) // Ngày giao dịch
//   budgetItemId String?  @db.Uuid // Liên kết với mục ngân sách nếu có
//   user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
//   account     Account   @relation(fields: [accountId], references: [id], onDelete: Cascade)
//   category    Category  @relation(fields: [categoryId], references: [id], onDelete: Cascade)
//   budgetItem  BudgetItem? @relation(fields: [budgetItemId], references: [id], onDelete: SetNull)

//   @@index([userId])
//   @@index([accountId])
//   @@index([categoryId])
// }

// model Limit {
//   id          String   @id @default(uuid()) @db.Uuid
//   userId      String   @db.Uuid
//   categoryId  String   @db.Uuid
//   amount      Decimal  @db.Decimal(13,2) // Giới hạn chi tiêu
//   period      LimitPeriod  // Hàng tháng, hàng năm,...
//   createdAt   DateTime @default(now())
//   updatedAt   DateTime @updatedAt
//   user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
//   category    Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

//   @@index([userId])
//   @@index([categoryId])
// }

// enum LimitPeriod {
//   DAILY
//   WEEKLY
//   MONTHLY
//   YEARLY
// }
